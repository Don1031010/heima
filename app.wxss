@import "./styles/iconfont.wxss";

/* mini-program does not support '*' selector */
page, view, text, swiper, swiper-item, image, navigator {
  padding: 0;
  margin: 0;
  box-sizing: border-box;
}

/* theme color */
page {
  --themeColor: #eb4450;
  font-size: 28rpx;
}